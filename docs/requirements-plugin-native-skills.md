Create a csx Codex-native plugin artifact in a new subfolder under /home/ubuntu/work/oh-my-codex. Use only Codex default plugin/skill capabilities: no custom runner, no CLI, no hooks, no MCP server, no app, no native agents. Preserve about 80% of the original OMX workflow value while reducing token overhead and installation/use friction. Plugin name must be csx and unrelated to OMX branding. Include skills exactly: analyze, spec (from deep-interview), plan, plan-pro (from ralplan), start-goal (from ultragoal), code-review. Include ultraqa and ai-slop-cleaner only if necessary; do not include them if the six skills do not depend on them. Store all deliverables in one new subfolder. Validate the plugin and skill structure with CLI evidence.
