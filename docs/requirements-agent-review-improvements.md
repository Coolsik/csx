Improve the csx Codex-native skill plugin by reintroducing the useful parts of the original OMX multi-agent workflows. Keep csx unrelated to OMX and keep it easy to install/use. Preserve skill-only Codex default plugin behavior: no hooks, no custom runner, no separate CLI, no MCP server, no app, no plugin-bundled native agents. Update the six csx skills so work is not always done by one agent: add lightweight Codex subagent review/delegation guidance inspired by the original OMX analyze, deep-interview/spec, plan, ralplan, ultragoal/start-goal, and code-review flows. Specifically keep token cost bounded, use critic/architect review only where it materially improves quality, define fallbacks when subagents are unavailable, validate plugin/skill structure, scan for forbidden surfaces/branding, and provide evidence.
