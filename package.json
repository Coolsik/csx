{
  "name": "@coolsik/csx",
  "version": "0.1.0",
  "description": "Codex-native workflow skills, agents, and prompt routing",
  "type": "module",
  "bin": {
    "csx": "./bin/csx.js"
  },
  "files": [
    "bin",
    "lib",
    "payload",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/csx.js && node --check lib/codex-models.js && node --check lib/install.js && node --check lib/installation-state.js && node --check lib/presets.js && node --check lib/setup.js && node --check lib/transaction-lock.js && node --check lib/transaction.js && node --check payload/hooks/csx-hook.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "optionalDependencies": {
    "fs-ext-extra-prebuilt": "2.2.9"
  },
  "license": "MIT",
  "author": "coolsik",
  "repository": {
    "type": "git",
    "url": "https://github.com/coolsik/csx.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "node-pty": "^1.1.0"
  }
}
